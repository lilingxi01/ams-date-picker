import { styled } from '../support/stitches.config';
import Icon from './svg/icon.svg';

export const AmsIcon = styled(Icon, {
  width: 38,
  height: 32,
  userSelect: 'none',
  pointerEvents: 'none',
});
